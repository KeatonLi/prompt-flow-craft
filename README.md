# 🚀 Prompt Flow Craft

**智能提示词工程平台 | 基于系统架构思维的 AI 提示词生成引擎**

[![Java](https://img.shields.io/badge/Java-17+-orange.svg)](https://www.oracle.com/java/)
[![Spring Boot](https://img.shields.io/badge/Spring%20Boot-3.2.0-brightgreen.svg)](https://spring.io/projects/spring-boot)
[![Vue](https://img.shields.io/badge/Vue-3.0+-4FC08D.svg)](https://vuejs.org/)
[![License](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)

---

## ✨ 核心特性

### 🎯 智能提示词生成
基于**系统架构思维**的四层架构模型，自动生成高质量、结构化的 AI 提示词：
- 核心定义层：角色建模 + 目标定义
- 交互接口层：输入规范 + 输出格式
- 内部处理层：工作流程 + 决策逻辑
- 全局约束层：安全边界 + 性能优化

### 💾 智能缓存
- 相同请求参数自动缓存，节省 API 调用成本
- 哈希算法生成缓存键，精确匹配
- 缓存命中统计，实时监控缓存效果

### 🏷️ 自动分类与标签
- **混合分类策略**：规则匹配 + LLM 智能分类
- 8大预设分类：写作、编程、数据、设计、商业、教育、翻译、对话
- 自动提取关键词作为标签
- 支持手动分类和标签管理

### 📚 历史记录管理
- 完整的生成历史记录
- 收藏功能，标记重要提示词
- 分页查询，支持多维度筛选
- 一键复用历史参数

### ⚡ 流式输出
- Server-Sent Events (SSE) 流式输出
- 打字机效果，实时展示生成过程
- 支持中断生成

---

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────────┐
│                        前端层 (Vue 3)                        │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │  提示词编辑器 │  │  历史面板    │  │    分类导航         │  │
│  └─────────────┘  └─────────────┘  └─────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                      API 网关层 (Spring Boot)               │
└─────────────────────────────────────────────────────────────┘
                              │
          ┌───────────────────┼───────────────────┐
          ▼                   ▼                   ▼
┌─────────────────┐ ┌─────────────────┐ ┌─────────────────────┐
│  生成服务       │ │  缓存服务       │ │   分类服务          │
│  - 同步生成    │ │  - 缓存存取    │ │   - 规则分类       │
│  - 流式生成    │ │  - 哈希键生成  │ │   - LLM分类        │
└─────────────────┘ └─────────────────┘ └─────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────────┐
│                      AI 模型层 (通义千问)                    │
└─────────────────────────────────────────────────────────────┘
```

---

## 🚀 快速开始

### 环境要求

| 组件 | 最低版本 |
|------|----------|
| Java | 17 |
| Maven | 3.6+ |
| Node.js | 18 |
| MySQL | 8.0+ |

### 1. 克隆项目

```bash
git clone https://github.com/your-username/prompt-flow-craft.git
cd prompt-flow-craft
```

### 2. 配置数据库

创建数据库：
```sql
CREATE DATABASE prompt_flow_craft CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 3. 配置 AI 模型

编辑 `src/main/resources/application.yml`：

```yaml
api:
  key: your-qwen-api-key
  base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
  model: qwen-plus
  temperature: 0.7
  max-tokens: 2000

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/prompt_flow_craft
    username: root
    password: your_password
```

### 4. 启动后端

```bash
mvn spring-boot:run
```

后端服务启动在 `http://localhost:8080`

### 5. 启动前端

```bash
cd frontend
npm install
npm run dev
```

前端服务启动在 `http://localhost:3000`

---

## 📡 API 接口

### 生成提示词

```bash
# 同步生成
POST /api/generate-prompt
Content-Type: application/json

{
  "taskDescription": "写一篇关于AI的文章",
  "targetAudience": "general",
  "outputFormat": "text",
  "tone": "professional",
  "length": "medium"
}
```

```bash
# 流式生成
POST /api/generate-prompt/stream
```

### 历史记录

```bash
# 获取历史记录
GET /api/history?page=1&size=20

# 切换收藏
POST /api/history/{id}/favorite

# 删除历史
DELETE /api/history/{id}
```

### 分类管理

```bash
# 获取所有分类
GET /api/categories

# 获取所有标签
GET /api/tags
```

---

## 📁 项目结构

```
prompt-flow-craft/
├── src/main/java/com/promptflow/
│   ├── controller/          # REST API 控制器
│   │   ├── PromptController.java      # 提示词生成
│   │   ├── HistoryController.java     # 历史记录
│   │   ├── CategoryController.java    # 分类管理
│   │   └── TagController.java         # 标签管理
│   ├── service/             # 业务逻辑
│   │   ├── PromptGenerationService.java   # 生成服务
│   │   ├── PromptCacheService.java        # 缓存服务
│   │   ├── PromptClassificationService.java # 分类服务
│   │   └── PromptHistoryService.java      # 历史服务
│   ├── entity/              # 数据实体
│   ├── repository/         # 数据访问
│   └── dto/                 # 数据传输对象
├── frontend/
│   └── src/
│       ├── views/           # 页面组件
│       ├── components/      # 通用组件
│       ├── api/             # API 接口
│       └── stores/          # 状态管理
└── pom.xml
```

---

## 🛠️ 技术栈

### 后端
- Java 17+
- Spring Boot 3.2.0
- Spring Data JPA
- MySQL 8.0+
- 通义千问 API

### 前端
- Vue 3.4+
- TypeScript
- Vite 4.0+
- Pinia (状态管理)

---

## 📝 功能截图

### 提示词编辑器
- 任务描述输入
- 目标受众、输出格式、语调风格、内容长度选择
- 约束条件和参考示例
- 一键生成、流式展示、复制功能

### 历史面板
- 最近生成历史
- 分类筛选
- 收藏功能
- 一键复用

---

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

---

## 📄 许可证

MIT License
